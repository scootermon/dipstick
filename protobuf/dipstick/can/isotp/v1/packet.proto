syntax = "proto3";
package dipstick.can.isotp.v1;

import "google/protobuf/timestamp.proto";

// ISO-TP data packet
message Packet {
  // Payload
  bytes data = 1;
  // Timestamp when the packet was received. Ignored when sending.
  google.protobuf.Timestamp timestamp = 2;
}
